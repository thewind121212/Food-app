import React from "react";
import Header from "./components/Layout/Header";

const App = (props) => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
};
export default App;
